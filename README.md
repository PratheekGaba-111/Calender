# Interactive Wall Calendar

The Next.js project lives in `calendar/` (the workspace root folder name has capital letters, which violates npm package naming rules).

## Run
```bash
cd calendar
pnpm install
pnpm dev
```

## Test
```bash
cd calendar
pnpm test
```

